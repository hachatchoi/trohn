# Kiến trúc hệ thống dự kiến

React → REST API Spring Boot → MySQL.

Backend chia module theo chức năng; mỗi module sẽ có Controller, Service, Repository, Entity và DTO khi triển khai.

Chưa có mã nguồn hoặc cấu hình hoạt động.

